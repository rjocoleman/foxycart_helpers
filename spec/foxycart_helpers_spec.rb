require 'spec_helper'

describe FoxycartHelpers do
  it 'has a version number' do
    expect(Foxycart::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
